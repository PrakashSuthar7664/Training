const { Buffer } = require("buffer");
const { buffer } = require("stream/consumers");
//creation of buffer

// const buf = Buffer.alloc(5)
// const buf = Buffer.alloc(5, 'Prakash')
// const buf = Buffer.alloc(5, 'Prakash' , 'base64')  //<Buffer 3e b6 a4 6a c8>

// console.log(buf);

// const buf = Buffer.from("Prakash")
// console.log(buf);

// const buf = Buffer.allocUnsafe(10);
// console.log(buf);

// write buffer

// const buf = Buffer.alloc(10)
// buf.write("hello")
// console.log(buf.toString())

// const buf = Buffer.from("hello");
// const buf2 = Buffer.alloc(5);
// buf.copy(buf2);
// console.log(buf2.toString());

// const buf3 = Buffer.concat([buf,buf2])
// console.log(buf3.toString());

// // console.log(buf.toString());

// console.log(buf.length);
// console.log(buf.slice(0, 3).toString());

// const buf = Buffer.alloc(10 ,"ABCD") ;
// const buf1 = Buffer.alloc(10 , "ABCD");
// console.log(buf.compare(buf1));
// console.log(Buffer.isBuffer(buf));

// const buf = Buffer.alloc(10) ;
// console.log(buf.fill('A').toString());

// const buf = Buffer.from(new String("Node.js Buffers are cool!"));
// console.log(buf.toString());
// console.log(buf.slice(0, 11).toString());
// console.log(buf.toJSON()) ;

// const buf1 = Buffer.from([0x1, 0x2, 0x3, 0x4, 0x5, 0x6, 0x7, 0x8]);
// console.log(buf1);
// buf1.swap16();
// console.log(buf1);







